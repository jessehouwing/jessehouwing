name: Update Scrum Classes
on:
  schedule: # Run workflow automatically
    - cron: '18 18 * * *' 
  workflow_dispatch:
  
jobs:
  update-readme-with-scrumclasses:
    name: Update GitHub profile
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: Update classes on GitHub profile
        uses: jessehouwing/jessehouwing/.github/actions/update-scrum-classes@60fca7b2bef77f0ce52b8d6a720fe126202c1222 # main
        with:
          trainer_id: 76
          user_agent: 'JesseHouwingScript'
  update-blog-with-scrumclasses:
    runs-on: ubuntu-latest
    name: Update Ghost Blog
    steps:
      - name: Update classes on Ghost Blog
        uses: jessehouwing/jessehouwing/.github/actions/update-scrum-classes-on-ghost@60fca7b2bef77f0ce52b8d6a720fe126202c1222 # main
        with:
          trainer_id: 76
          user_agent: 'JesseHouwingScript'
          ghost_token: ${{ secrets.GHOST_TOKEN }}
